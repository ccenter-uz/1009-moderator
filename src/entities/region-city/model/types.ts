export interface RegionsType {
  result: {
    data: { id: string; name: string }[];
    totalDocs: number;
  };
}

export interface CitiesType {
  result: {
    data: { id: string; name: string }[];
    totalDocs: number;
  };
}
